import { Component, OnInit} from '@angular/core';

@Component({
  selector: 'app-structure',
  templateUrl: './structure.html',
})
export class StructureComponent {
  constructor() {
  }

  ngOnInit() {
    
    // history.pushState({}, "home", "http://localhost:4200/");
    // history.pushState({}, "login", "http://localhost:4200/");
    // history.pushState({}, "info", "http://localhost:4200/");
    // history.pushState({}, "home", "http://localhost:4200/");
  }
  
}
